# Server settings
server.port=8083
spring.application.name=itemCenter

# Database settings
spring.datasource.url=jdbc:mysql://localhost:3306/itemcenter?useSSL=false&characterEncoding=utf-8
spring.datasource.username=root
spring.datasource.password=hot365fm
spring.datasource.driver-class-name=com.mysql.jdbc.Driver

# MyBatis settings
mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.type-aliases-package=com.mcp.item.entity

# Logging
logging.level.root=INFO
logging.level.com.mcp.item=DEBUG

# Dubbo settings
dubbo.application.name=itemCenter
dubbo.protocol.name=dubbo
dubbo.protocol.port=20883
dubbo.registry.address=N/A 