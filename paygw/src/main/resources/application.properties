# Server settings
server.port=8084
spring.application.name=paygw

# Database settings
spring.datasource.url=jdbc:mysql://localhost:3306/paygw?useSSL=false&characterEncoding=utf-8
spring.datasource.username=root
spring.datasource.password=hot365fm
spring.datasource.driver-class-name=com.mysql.jdbc.Driver

# MyBatis settings
mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.type-aliases-package=com.mcp.payment.entity

# Logging
logging.level.root=INFO
logging.level.com.mcp.payment=DEBUG

# Dubbo settings
dubbo.application.name=paygw
dubbo.protocol.name=dubbo
dubbo.protocol.port=20884
dubbo.registry.address=N/A 