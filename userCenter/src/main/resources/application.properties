# 应用名称
spring.application.name=user-center

# 服务端口
server.port=8081

# 日志配置
logging.level.root=INFO
logging.level.com.mcp=DEBUG

# Database Configuration
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/usercenter?useSSL=false&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
spring.datasource.username=root
spring.datasource.password=hot365fm
spring.datasource.driver-class-name=com.mysql.jdbc.Driver

# MyBatis Configuration
mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.type-aliases-package=com.mcp.user.entity
mybatis.configuration.map-underscore-to-camel-case=true

# Logging Configuration
logging.level.com.mcp.user.mapper=debug 