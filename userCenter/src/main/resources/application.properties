# Server settings
server.port=8080
spring.application.name=userCenter

# Database settings
spring.datasource.url=jdbc:mysql://localhost:3306/usercenter?useSSL=false&characterEncoding=utf-8
spring.datasource.username=root
spring.datasource.password=hot365fm
spring.datasource.driver-class-name=com.mysql.jdbc.Driver

# MyBatis settings
mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.type-aliases-package=com.mcp.user.entity

# Logging
logging.level.root=INFO
logging.level.com.mcp.user=DEBUG

# Dubbo settings
dubbo.application.name=userCenter
dubbo.protocol.name=dubbo
dubbo.protocol.port=20880
dubbo.registry.address=N/A 