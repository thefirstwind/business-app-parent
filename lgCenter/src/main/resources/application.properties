# Server settings
server.port=8082
spring.application.name=lgCenter

# Database settings
spring.datasource.url=jdbc:mysql://localhost:3306/lgcenter?useSSL=false&characterEncoding=utf-8
spring.datasource.username=root
spring.datasource.password=hot365fm
spring.datasource.driver-class-name=com.mysql.jdbc.Driver

# MyBatis settings
mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.type-aliases-package=com.mcp.logistics.entity

# Logging
logging.level.root=INFO
logging.level.com.mcp.logistics=DEBUG

# Dubbo settings
dubbo.application.name=lgCenter
dubbo.protocol.name=dubbo
dubbo.protocol.port=20882
dubbo.registry.address=N/A 